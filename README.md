# VLAD PAVLOV - Brand Manager Landing Page

Landing page for branding/marketing manager Vlad Pavlov

## 🚀 Деплой на GitHub Pages

Проект настроен для автоматического деплоя на GitHub Pages с использованием Gulp.

### Настройка GitHub Pages

1. Перейдите в настройки репозитория: `Settings` → `Pages`
2. В разделе `Build and deployment` → `Source` выберите:
   - **Source**: `GitHub Actions` (рекомендуется для автоматического деплоя)
   
   ИЛИ для ручного деплоя:
   - **Source**: `Deploy from a branch`
   - **Branch**: `main` или `master`
   - **Folder**: `/docs`

### Автоматический деплой через GitHub Actions

Проект использует GitHub Actions для автоматического деплоя:
- При каждом push в ветку `main` или `master` автоматически запускается сборка через Gulp
- Собранные файлы автоматически деплоятся на GitHub Pages

### Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера с автоматической перезагрузкой
npm run dev

# Сборка для продакшена
npm run build

# Очистка папки docs
npm run clean

# Просмотр собранной версии
npm run preview
```

### Структура проекта

- `index.html` - главная страница
- `less/` - исходные LESS файлы (компилируются в CSS)
- `js/` - JavaScript файлы (объединяются и минифицируются)
- `img/`, `fonts/` - статические ресурсы
- `docs/` - собранная версия для GitHub Pages (генерируется автоматически через Gulp)

### Сборка проекта

Проект использует **Gulp** для сборки:

1. **LESS → CSS**: Компиляция LESS файлов в CSS с автопрефиксером и минификацией
2. **JavaScript**: Объединение всех JS файлов в `bundle.js` и минификация
3. **Статические файлы**: Копирование HTML, изображений и шрифтов
4. **Результат**: Все файлы собираются в папку `docs/` для GitHub Pages

### Gulp задачи

- `gulp build` - полная сборка проекта
- `gulp serve` - запуск dev-сервера с BrowserSync
- `gulp clean` - очистка папки docs
- `gulp styles` - компиляция LESS
- `gulp scripts` - обработка JavaScript
- `gulp images` - копирование изображений
- `gulp fonts` - копирование шрифтов
